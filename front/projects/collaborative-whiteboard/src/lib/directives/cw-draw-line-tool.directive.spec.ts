import { CwDrawLineToolDirective } from './cw-draw-line-tool.directive';

describe('CwDrawLineToolDirective', () => {
  it('should create an instance', () => {
    const directive = new CwDrawLineToolDirective();
    expect(directive).toBeTruthy();
  });
});
