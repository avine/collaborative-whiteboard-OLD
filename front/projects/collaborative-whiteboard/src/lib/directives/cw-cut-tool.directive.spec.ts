import { CwCutToolDirective } from './cw-cut-tool.directive';

describe('CwCutToolDirective', () => {
  it('should create an instance', () => {
    const directive = new CwCutToolDirective();
    expect(directive).toBeTruthy();
  });
});
