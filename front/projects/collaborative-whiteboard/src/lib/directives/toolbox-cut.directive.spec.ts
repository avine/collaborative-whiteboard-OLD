import { ToolboxCutDirective } from './toolbox-cut.directive';

describe('ToolboxCutDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolboxCutDirective();
    expect(directive).toBeTruthy();
  });
});
