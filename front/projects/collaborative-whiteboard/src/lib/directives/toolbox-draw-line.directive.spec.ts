import { ToolboxDrawLineDirective } from './toolbox-draw-line.directive';

describe('ToolboxDrawLineDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolboxDrawLineDirective();
    expect(directive).toBeTruthy();
  });
});
